<h1>Editing service</h1>

<%= form_for(@service, remote: true) do |f| %>
	<%= render 'form', f: f %>
<% end %>

<hr>
<p>
	<%= link_to 'Show', @service, class: 'btn btn-sx btn-default' %>
	<%= link_to 'Back', services_path, class: 'btn btn-sx btn-default' %>
</p>
